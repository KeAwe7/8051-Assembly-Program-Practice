MOV DPTR, #4009H
MOV R0, #40H
MOV R1, #6

MOVE:	MOVX A, @DPTR
	MOV @R0, A
	INC DPTR
	INC R0
	DJNZ R1, MOVE

MOV A, #00H
MOV R1, #6
DEC R0

FIND:
	MOV B, @R0
	CJNE A, B, DOWN

DOWN:	JNC SKIP
	MOV A, B
SKIP:	DEC R0
	DJNZ R1, FIND

MOV DPTR, #4184H
MOVX @DPTR, A

END	